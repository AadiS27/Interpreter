// Recursive function that subtracts 1 from n until it reaches 0
 fun subtract(n) {
     if (n == 0) {
         return;
     }
     write(n);
     subtract(n - 1);  // Recursive call with n - 1
 }
 
 // Start the recursion with 5
 var num = 5;
 subtract(num);